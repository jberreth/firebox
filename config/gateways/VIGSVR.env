# =============================================================================
# VIGSVR Gateway Configuration
# Server Gateway - Main server and backend operations
# =============================================================================

# Gateway Identity
GATEWAY_NAME=VIGSVR
GATEWAY_DISPLAY_NAME=VIG Server
GATEWAY_DESCRIPTION=Server gateway for backend operations and data processing
GATEWAY_ENVIRONMENT=production

# Container Configuration
CONTAINER_NAME=ignition-sandbox_vigsvr_1
CONTAINER_HOSTNAME=vigsvr
SERVICE_NAME=vigsvr

# Port Configuration (IP-based allocation)
HTTP_PORT=8072
HTTPS_PORT=8472
GAN_PORT=8672
TRACE_PORT=10072
METRICS_PORT=11072

# =============================================================================
# IGNITION GATEWAY CONFIGURATION
# =============================================================================

# Core Ignition Settings
ACCEPT_IGNITION_EULA=Y
TZ=America/Boise
GATEWAY_ADMIN_USERNAME=admin
GATEWAY_ADMIN_PASSWORD=password
DISABLE_QUICKSTART=true
GATEWAY_SYSTEM_NAME=VIGSVR
GATEWAY_USESSL=false
GATEWAY_NETWORK_AUTOACCEPT_DELAY=3
GATEWAY_DEBUG_ENABLED=1
GATEWAY_MAX_MEMORY=1024
IGNITION_DATASOURCE_AUTOSTART=false
IGNITION_MODULES_ENABLED=false
IGNITION_DEVICE_CONNECTIONS_ENABLED=false
IGNITION_OPC_UA_SERVER_ENABLED=false

# =============================================================================
# GATEWAY-TO-GATEWAY CONNECTIONS (VIGSVR has no specific connections defined)
# =============================================================================

# No specific gateway connections defined for VIGSVR in the provided configuration